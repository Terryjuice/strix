services:
  - type: web
    name: strix-scanner
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python strix_web_panel.py"
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: PORT
        value: 10000
